---

- name: install Debian ssh package
  apt:
    name: ssh
    state: latest
  when: ansible_os_family == "Debian"

- name: install RedHat ssh packages
  yum:
    name: "{{item}}"
    state: latest
  with_items:
    - openssh-server
    - openssh-clients
  when: ansible_os_family == "RedHat"
